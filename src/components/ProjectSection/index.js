import React from 'react'

export default function ProjectSection(props) {
    return (
        <section class="section white hide-on-med-and-down">
            {props.children}
        </section>
    )
}
