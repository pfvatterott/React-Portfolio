import React from 'react'

export default function MobileProjectSection(props) {
    return (
        <section class="section white hide-on-large-only">
            {props.children}
        </section>
    )
}
